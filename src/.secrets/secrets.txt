In this folder you can setup your sensitive data. 

Always make sure to list this folder in your .gitignore

for extra protection use the following extensions:
    .env
    .venv
    .secrets 

example: .env file

API_KEY=dummy-key
PROJECT_ID=flex_templates